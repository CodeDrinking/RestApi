let Joi,User,CustomErrorHandler,bcrypt,jwtService;_535‍.x([["default",()=>_535‍.o]]);_535‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_535‍.w("../../models",[["User",["User"],function(v){User=v}]]);_535‍.w("../../Services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_535‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_535‍.w("../../Services/jwtService",[["default",["jwtService"],function(v){jwtService=v}]]);





const RegisterController ={
    async register (req , res , next){





        const registerSchema = Joi.object({
            name :  Joi.string().min(3).max(30).required(),
            email :  Joi.string().email().required(),
            password :  Joi.string().pattern(new RegExp('[^a-zA-Z0-9]{3,30}$')),
            repeat_password :  Joi.ref('password')
        });

        const {error} =registerSchema.validate(req.body);

        if(error){
            return next(error)
        }
//check if user alredy in database
         try{ 
            const exist =  await User.exists({email : req.body.email});

            if(exist){
                return next(CustomErrorHandler.alreadyExists('this email is alredy taken'))
            }
        }
         catch(err){
            return next(err)
    
       }


       //hash password
       const hashedPassword = await bcrypt.hash(req.body.password ,10);

       //prepare the model
       const user  = new User({
        name : req.body.name,
        email : req.body.name,
        password : hashedPassword
       })

       let access_Token;
       try{
        const result = await user.save();

        //Token
        access_Token= jwtService.sign({_id:result._id ,role:result.role})
       }


        catch{
        return next(err);
        }
  

    
         res.json( { access_Token : access_Token })
        
    }
}


_535‍.d(RegisterController);